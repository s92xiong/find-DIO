import React from 'react';

function UserScore({ highScores, winTime, setInputHighScore }) {

  const handleInputFieldChange = (e) => setInputHighScore(e.target.value.toUpperCase());

  return <div></div>
}

export default UserScore;